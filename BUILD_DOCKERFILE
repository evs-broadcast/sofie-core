# syntax=docker/dockerfile:1

FROM ubuntu:22.04

RUN apt update
RUN apt install -y curl openjdk-11-jdk \
  && curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash - \
  && source $HOME/.bashrc \
  && nvm install 18 \
  && nvm alias default 18 \
  && nvm use default
